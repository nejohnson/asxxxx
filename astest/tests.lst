# Cases are run in this order.  A case id is at most 8 characters:
# the working directory it creates must keep to an 8.3 name.
ab_ts
ab_tm
ab_tbm
radix
expr

# Held back, both blocked by faults this harness found in the tree:
#
#   equtst  ascheck -loxff equtst segfaults in newdot(), asmain.c:3373,
#           with dot.s_area NULL.  Present in the pristine 6.10 drop.
#   bndry   ascheck/bndry/b_areas_*.asm reference s_Area_2 and
#           b_Area_2_1, which are not names 6.10 generates;  the
#           linker reports them undefined.  Stale test source.
